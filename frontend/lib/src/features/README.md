# Feature Structure

Each ERP feature should follow this shape:

- `data`: remote/local data sources, DTOs, repository implementations
- `domain`: entities, value objects, repository contracts, use cases
- `presentation`: pages, widgets, controllers/providers

Current planned features:

- auth
- dashboard
- students
- fees
- hostel
- attendance
- reports
- settings
